# 📂 安提斯特虚拟国 - 项目结构说明

## 📂 根目录 (Root)
| 文件名 | 说明 |
| :--- | :--- |
| `common.php` | **核心**：定义数据库连接、公共函数、CORS 和安全头。 |
| `init.php` | **初始化**：系统首次运行时调用，创建管理员账号 (admin/test) 和必要索引。 |
| `retinbox-config.js` | **前端配置**：定义 API 请求类 (RetinboxAPI) 和全局配置。 |
| `init-data.js` | **引导脚本**：页面加载时检查系统是否初始化，未初始化时自动引导。 |
| `tailwind.css` | 样式库文件。 |
| `*.html` | 各个前端页面 (index, bank, library, user-center, admin)。 |

## 📂 auth (认证模块)
| 文件名 | 说明 |
| :--- | :--- |
| `register.php` | 用户注册。 |
| `login.php` | 用户登录 (写入 Session)。 |
| `logout.php` | 退出登录 (清除 Session)。 |
| `changePassword.php` | 修改当前用户密码。 |
| `getCurrentUser.php` | 获取当前登录用户信息 (用于前端鉴权)。 |

## 📂 users (用户管理)
| 文件名 | 说明 |
| :--- | :--- |
| `getUsers.php` | (管理员) 获取所有用户列表。 |
| `getUser.php` | 获取单个用户信息。 |
| `updateUser.php` | 更新用户资料 (基本信息、简介等)。 |
| `deleteUser.php` | (管理员) 删除用户。 |
| `upload-avatar.php` | 上传用户头像。 |
| `avatar-file.php` | 读取/显示头像文件。 |

## 📂 bank (银行系统)
| 文件名 | 说明 |
| :--- | :--- |
| `createAccount.php` | (旧接口/备用) 创建基础账户。 |
| `createCard.php` | **核心**：开通新银行卡 (借记卡/信用卡)。 |
| `getAccount.php` | 获取当前用户的基础账户信息。 |
| `getAllCards.php` | (管理员) 获取所有用户的卡片列表。 |
| `getCards.php` | 获取当前用户的卡片列表。 |
| `updateCard.php` | (管理员) 修改卡片状态或额度。 |
| `deleteCard.php` | 注销银行卡。 |
| `getTransactions.php` | 获取交易流水 (存款/取款/转账)。 |
| `transfer.php` | **核心**：转账接口 (卡对卡)。 |
| `repayment.php` | **核心**：信用卡还款接口。 |
| `updateBalance.php` | (管理员) 调账/存取款接口。 |
| `searchRecipient.php` | 搜索转账接收人。 |

## 📂 library (图书馆)
| 文件名 | 说明 |
| :--- | :--- |
| `getDocuments.php` | 获取文献列表 (支持搜索/过滤)。 |
| `saveDocuments.php` | (管理员) 保存文献列表 (用于删除/审核)。 |
| `upload-document.php` | 上传新文献。 |
| `document-file.php` | 读取/下载文献文件。 |
| `addRecord.php` | (保留) 借阅记录相关。 |
| `getRecords.php` | (保留) 获取借阅记录。 |
| `returnBook.php` | (保留) 归还图书。 |

## 📂 admin (后台管理)
| 文件名 | 说明 |
| :--- | :--- |
| `getAdminPermissions.php` | 获取管理员权限列表。 |
| `saveAdminPermissions.php` | 保存管理员权限配置。 |
| `getAdminLogs.php` | 获取后台操作日志。 |
| `saveAdminLogs.php` | 记录后台操作日志。 |
| `getPermissionTemplates.php`| 获取权限模板。 |
| `savePermissionTemplates.php`| 保存权限模板。 |

## 📂 settings (设置)
| 文件名 | 说明 |
| :--- | :--- |
| `getSettings.php` | 获取用户偏好设置。 |
| `updateSettings.php` | 更新用户偏好设置。 |